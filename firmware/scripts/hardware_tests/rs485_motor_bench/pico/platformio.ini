; RS-485 motor bench — Raspberry Pi Pico master.

[platformio]
default_envs = pico

[env:pico]
platform  = raspberrypi
board     = pico
framework = arduino
monitor_speed = 115200

build_flags =
    -I${PROJECT_DIR}/src
    -I${PROJECT_DIR}/../
    -I${PROJECT_DIR}/../../
